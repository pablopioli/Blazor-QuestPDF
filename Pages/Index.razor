@page "/"

<PageTitle>Index</PageTitle>

<h1>Hello, world!</h1>

<embed src="@ThePDF" width="1500" height="2000" />

@code {
    string ThePDF;

    public Index()
    {
        var bytes = BuildDocument();
        ThePDF = "data:application/pdf;base64," + Convert.ToBase64String(bytes);
    }
}
